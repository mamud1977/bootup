name: CI/CD Pipeline - Count Files in Folder

on:
  push:
    branches:
      - branch1

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a simple message
        run: echo "Hello from GitHub Actions!"

      - name: Echo an environment variable
        run: echo "The current branch is ${{ github.ref }}"

